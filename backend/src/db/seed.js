export async function seedInitialData() {
  // Semilla opcional: en esta base inicial no hay modelos definidos aún.
  // Deja este stub para evitar errores de import y para poder ampliar luego.
  return;
}

